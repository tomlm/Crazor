@using Crazor
@using CrazorDemoBot.Cards.Dice
@inherits CardView<DiceApp>

@functions {
    public void OnInitialize()
    {
        if (App.Dice != null)
            ShowCard("Roll");
        else
            ShowCard("Settings");
    }
}