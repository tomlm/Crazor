@inherits CardView
@attribute [CommandInfo("Post card", "Post a adaptive card JSON directly", Context="message,compose,commandBox")]
@attribute [TaskInfo("Post Card", "large", "large")]

<Card Version="1.4" MinHeight="600px">
</Card>

@code {
    public string CardId { get; set; } = Guid.NewGuid().ToString("n");

    public void OnLoad()
    {
        ReplaceView($"{CardId}/Edit");
    }
}