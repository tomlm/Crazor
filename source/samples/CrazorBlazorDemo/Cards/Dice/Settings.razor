@using Crazor
@using CrazorBlazorDemo.Cards.Dice
@inherits CardView<DiceApp>

<Card Version="1.5">
    <InputText Binding="App.DiceName" />
    <InputNumber Binding="App.NumberDice" />

    @if (IsModelValid)
    {
        <ActionExecute Verb="OnCancel" Title="Cancel" />
    }
    <ActionExecute Verb="OnOK" Title="OK"/>
</Card>

@code {
    public void OnOK()
    {
        if (this.IsModelValid)
        {
            this.App.RollDice();
            this.CloseTaskModule(TaskModuleAction.Auto);
            CloseView();
        }
    }
}